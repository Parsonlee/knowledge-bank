---
type: "source"
tags:
  - "machine-learning"
  - "embedding"
  - "representation-learning"
summary: "探讨在多模型流水线中混用多个 Embedding 模型时的局限性，阐述了等维异构空间无法直接度量相似度的物理原因，并提出了特征拼接与对齐训练等解决方案。"
sources:
  - "raw/articles/2025-12-26_The-right-way-to-use-multiple-embedding-models_19b5c7.md"
updated: 2026-08-03
---

# 来源信息
- **标题**: The right way to use multiple embedding models
- **来源**: Daily Dose of DS
- **日期**: 2025-12-26
- **链接/ID**: 19b5c7637722a2ba

# 核心要点
- **等维嵌入的对比局限**：在机器学习流水线中，即使两个不同模型（如 BERT、XLNet 等）生成的嵌入表示维度相同，也**绝不能**直接计算它们的几何距离（如欧式距离）或余弦相似度。
- **空间异构性 (Out of Space)**：不同模型的基底向量（物理坐标轴）方向未对齐。以 3D 空间为例，如果两个嵌入在 z 轴对齐但 x/y 轴存在夹角旋转，直接按分量对比将完全失效。
- **混合嵌入的标准方案**：
  - **特征拼接 (Concatenation)**：将多个嵌入直接拼接为高维向量，这能有效保留各模型的全量特征，且支持不等维度的拼接。
  - **跨模型对齐训练 (Alignment Training)**：通过特定的损失函数约束，在共同数据上显式对齐不同模型的嵌入空间。

# 关联概念与实体
- [[wiki/concepts/概念_跨模型嵌入对比局限|概念：跨模型嵌入对比局限]]

# 关键引文
> This is because even though these embeddings have the same length (or dimensions), they are not in the same space, i.e., they are out of space.
> 
> Thus, the representations can NEVER be compared, unless generated by the same network or different networks that were explicitly trained with alignment.
> 
> Otherwise, concatenation is a much better way to leverage multiple embeddings.

---
> 📎 **物理文献**：[[raw/articles/2025-12-26_The-right-way-to-use-multiple-embedding-models_19b5c7.md]]
