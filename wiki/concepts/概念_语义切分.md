---
tags:
- RAG
- RAG/chunking
confidence: high
type: concept
summary: 文本切分五层级中的 Level 4。基于向量模型计算句子间的语义差异（向量距离），当差异超过阈值时进行拆分。不再依赖固定字符/token 数或文档格式，而是根据语义内容动态决定切分点。
created: '2026-07-06'
updated: '2026-08-03'
sources:
- wiki/sources/RAG技巧与底层代码剖析.md
- wiki/sources/RAG文本切分_JSON文档切分.md
- wiki/sources/RAG文本切分_Markdown切分.md
- wiki/sources/RAG文本切分_字符切分.md
- wiki/sources/RAG文本切分_语义切分.md
- wiki/sources/提升RAG问答质量的技术路线.md
- wiki/sources/2025-05-29_5-chunking-strategies-for-RAG_1971dc.md
---

# 概念_语义切分


## 定义

文本切分五层级中的 Level 4。基于向量模型计算句子间的语义差异（向量距离），当差异超过阈值时进行拆分。不再依赖固定字符/token 数或文档格式，而是根据语义内容动态决定切分点。

## 工作原理

1. 将文本按句子分割（作为语义基础块）。
2. 使用嵌入模型将每个句子转为向量表示。
3. 计算相邻句子之间的向量差异（例如余弦距离）。
4. 当差异值在特定位置大幅跃升并超过预设的阈值时，在此处进行拆分，生成语义上相对连贯和自洽的独立块。

## 四种阈值控制方法（breakpoint_threshold_type）

1. **百分位数（percentile）**：默认方式，拆分大于 X 百分位数的差异
2. **标准差（standard_deviation）**：拆分大于 X 个标准差的差值，粒度更粗
3. **四分位距（interquartile）**：使用四分位数距离作为拆分阈值
4. **梯度（gradient）**：距离的梯度与百分位数方法结合，适用于块彼此高度相关或特定领域场景；在梯度数组上应用异常检测，使分布更宽，易于识别高度语义数据中的边界

## 优缺点

- **优点**：能够跟随语言自然流动态切分，最大化保持意思完整，提升 RAG 检索阶段的准确度。
- **缺点**：切分结果强依赖于相似度阈值设定，阈值通常随文档性质不同而变动，且计算相邻句子向量会有额外的推理时间开销。

## 实现

- LangChain（experimental）：`SemanticChunker(embedding_model, breakpoint_threshold_type="percentile|standard_deviation|interquartile|gradient")`
- 需指定嵌入模型（OpenAIEmbeddings 或自定义模型）

## 关联

- 上层框架：[[概念_文本切分五层级]]
- 相关概念：[[概念_Embedding与向量检索]]、[[概念_递归字符切分]]
- 来源：[[wiki/sources/RAG文本切分_语义切分]]、[[wiki/sources/2025-05-29_5-chunking-strategies-for-RAG_1971dc]]