---
type: concept
tags:
- Infra/AI
summary: 本概念定义并横向对比了 CPU、GPU、TPU、NPU 与 LPU 五种主流 AI 计算芯片架构的物理特征、存储层次以及计算调度机制，并分析其在
  AI 训练与推理场景中的物理限制与权衡。
sources:
- wiki/sources/2026-07-28_CPU-vs-GPU-vs-TPU-vs-NPU-vs-LPU_19faa9.md
updated: '2026-08-04'
---

# 概念：AI硬件加速芯片架构

## 定义
AI硬件加速芯片架构是指针对人工智能（尤其是深度学习的矩阵乘法与高并发张量计算）设计的多样化处理器物理架构。计算芯片从通用灵活性（CPU）逐步走向极端专用化（LPU），以在灵活性、并行度以及内存访问带宽之间取得不同的物理折衷。

## 五大芯片架构解构

### 1. CPU (Central Processing Unit)
- **物理特征**：由少量极其强悍的物理核心组成，配备了极其复杂的指令译码、分支预测、乱序执行逻辑，并占用大量片上面积用于多级缓存（L1/L2/L3 SRAM）。
- **存储层次**：核心通过深层 Cache 架构与外部板载通用主存（DRAM）交互。
- **调度机制**：硬件级复杂的动态调度与分支预测。
- **权衡与适用**：具备极致的通用控制灵活性。但对于深度学习中简单且重复的矩阵乘法而言，其算力利用率和访存带宽完全不足，不适合直接用于大规模 AI 训练或高吞吐推理。

### 2. GPU (Graphics Processing Unit)
- **物理特征**：极大弱化了复杂的控制逻辑与分支预测，将芯片面积主要分给成千上万个轻量化的小计算核心。采用 SIMD/SIMT（单指令多数据/多线程）架构。
- **存储层次**：配备片上寄存器、SRAM（Shared Memory）和极宽位宽的外部高速显存（HBM 或 GDDR）。
- **调度机制**：硬件级的轻量级多线程调度。
- **权衡与适用**：超高的并发吞吐能力使其成为 AI 模型训练与大规模推理的行业主导芯片。然而，其巨大的片外显存（HBM）与片上 SRAM 之间的数据搬运速度限制了计算性能，成为典型的 Memory-bound 瓶颈。

### 3. TPU (Tensor Processing Unit)
- **物理特征**：Google 设计的神经网络专用集成电路（ASIC）。核心计算单元为二维相连的乘加器（MAC）网格——**脉动阵列（Systolic Array）**。
- **存储层次**：数据流以波浪传导形式在 MAC 阵列内部相邻寄存器间直接传递，无需每一轮计算都返回读取主存，极大缓解了访存压力。
- **调度机制**：**编译器静态编排（Compiler-Scheduled）**，彻底移除硬件运行时调度，零时钟周期浪费。
- **权衡与适用**：矩阵乘法性能极高，静态执行效率显著优于 GPU。但丧失了硬件通用性，过度依赖专用编译器生态，多用于 Google 云端的大模型训练与推理。

### 4. NPU (Neural Processing Unit)
- **物理特征**：面向边缘端、端侧（如智能手机、智能穿戴、智能车载）的低功耗 AI 推理芯片。围绕 Neural Compute Engine 构建，集成中等规模的 MAC 矩阵乘法阵列。
- **存储层次**：将 MAC 阵列与小容量片上 SRAM 紧密封装在一起，不配置昂贵的高带宽 HBM，而是采用手机/系统低功耗内存（LPDDR）。
- **调度机制**：硬编码的算子加速与编译器支持。
- **权衡与适用**：核心目标是在单位数瓦特（Single-digit Watt）的超低功耗预算下运行实时推理（如 Apple ANE、Intel NPU）。因其片上资源极度受限，不适用于大型语言模型的离线训练。

### 5. LPU (Language Processing Unit)
- **物理特征**：Groq 针对大语言模型极致低延迟推理提出的非冯·诺依曼架构（Tensor Streaming Processor）。
- **存储层次**：**彻底移除了外部显存（HBM / DRAM）**。将所有的模型权重和计算激活值完全放置于芯片上的大容量 SRAM 中（单片约 230MB）。
- **调度机制**：完全由编译器静态时间段精确排期，确保微秒级的计算指令和数据交互，无硬件动态缓存抖动。
- **权衡与适用**：消除了外部显存带宽瓶颈，实现零 Cache Miss，推理延迟极低。物理代价是单颗芯片的片上 SRAM 显存极其有限，在承载大模型时需要通过外部超高速互联接口将数百颗 LPU 芯片级联。

---

## 五大加速架构物理特性横向对比表

| 架构维度 | CPU | GPU | TPU | NPU | LPU |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **物理核/单元性质** | 少量复杂通用大核 | 数千个轻量级 SIMD 核心 | 乘加单元（MAC）组成的脉动阵列 | 片上 MAC 阵列与神经网络引擎 | 一维时空流式计算执行单元 |
| **片外内存介质** | 通用 DRAM | GDDR / HBM 高宽带显存 | HBM 高带宽显存 | 系统 LPDDR 低功耗内存 | 无（不依赖外部内存） |
| **片上暂存介质** | 多级 SRAM Cache | SRAM Shared Memory | SRAM Buffer | 片上 SRAM | 超大容量片上 SRAM (全部权重驻留) |
| **计算调度机制** | 硬件动态分支预测与乱序执行 | 硬件级轻量级 Warp 线程调度 | 编译器控制的静态编排数据流 | 专用驱动层与编译器排期 | 编译器周期级静态确定排期 |
| **物理核心权衡** | 极高灵活性，极低矩阵算力 | 中等灵活性，超高矩阵吞吐 | 专攻矩阵，无通用控制逻辑 | 低功耗高效率，内存位宽窄 | 零访存限制与延迟，片上容量极小 |
| **典型应用场景** | 通用计算、分支调度、数据库 | AI 模型大规模训练与推理 | 云端大模型高效训练与推理 | 智能手机、IoT 等边缘推理 | 大模型超低延迟推理服务 |

---

## 物理存储架构的演进与算法优化交互

在大模型时代，AI 加速芯片的效率不仅取决于算力峰值（TFLOPs），更取决于**存储架构（Memory Hierarchy）**的搬运效率。

根据 GPU 和 CPU 的存储层级设计，片上 SRAM 的读取速度是片外全局显存 HBM 的 8-15 倍。在传统的 GPU 架构中，自注意力机制（Self-Attention）计算需要在 HBM 和 SRAM 之间多次读写 $N \times N$ 的中间得分矩阵，造成严重的访存瓶颈（Memory-bound）。

为克服这一硬件局限，催生了 `[[概念_FlashAttention]]` 这种 I/O 感知的精确注意力算法：
- `[[概念_FlashAttention]]` 充分利用了 GPU 的片上高速 **SRAM**，通过**分块计算（Tiling）**和**在线 Softmax（Online Softmax）**机制，在 SRAM 内部完成注意力的局部块乘积并实时计算归一化分子分母。
- 这使得计算过程不需要在 HBM 中“物化”出完整的 $N \times N$ 矩阵，只需要在最后写回输出矩阵。它通过算法的重新排期，将内存密集型（Memory-bound）的注意力计算转变为计算密集型（Compute-bound）。

相比之下，**LPU** 的硬件设计则更为极端：
- LPU 直接从硬件设计上干掉了片外 HBM，把所有模型权重全部放入片上 SRAM。这意味着 LPU 在硬件级别消除了 SRAM-HBM 的搬运限制，这使得类似 `[[概念_FlashAttention]]` 这种针对 HBM 读写瓶颈而做的分块算法在其架构上失去了物理意义。
- **TPU** 的脉动阵列则通过数据在 MAC 二维网格中的“流动”来减少与内存（HBM）的重复交互，其硬件本身就具备减少 I/O 频繁往返的设计。

这表明，**AI 硬件的物理存储设计与底层算法工程存在深度的协同演进关系**：硬件物理极限决定了算法优化的方向（如 GPU 孕育了 FlashAttention），而极端的硬件重构（如 LPU）则可能直接从物理层消除特定算法优化的需求。
