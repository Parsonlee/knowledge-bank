1.  机器环境

shell：finalshell

2.  账号权限

IP：10.6.10.52

计算机名：yyw-sh-pc-1542

用户名：yyw

密码：yyw-1540

可以ssh登录了，切换到root: 输入sudo su，密码：yyw-1540

jupyter：<http://10.6.10.52:8888/tree> 账户密码同上

3.  工作内容

- 供应商对码现状了解

- 名称打标及错位问题汇总

- 模型训练及优化

[命名实体识别-基于对码数据](https://doc.weixin.qq.com/doc/w3_AT0AdAbcAO8o1ucjOw1RrGfk3p1Kc?scode=AEQA-QfUAA0AS58jbrAMsAlQbuAEo)

## 周报：5.8-5.12

1.  完成的工作：

    a.  完成对某个数据集中匹配结果的人工标注。

    b.  根据个人的分词规则，标注200条数据，微调预训练uie-base模型。

    c.  开放接口给对码小组。

    d.  对695条含有匹配结果人工标注的数据集进行抽取，然后将实体送入匹配模型得到结果，并于匹配结果和人工标注结果进行比对计算准确率。

    e.  修改200条微调数据的分词标注，重新训练uie-base，测试结果。

2.  会议总结：

    a.  AIGC会议：讨论了使用\`LLM+内部embedding
        database\`的技术方案。在其他同事实验现有技术路线的同时，调研其他现成的技术方案以及完成度较高的demo。

    b.  对码会议：讨论确定出一套较为标注的分词规则；持续跟进对码项目的进展，以确定实体识别模型在当前流程中的对接环节。

3.  之后的工作：

    a.  调整分词规则后的uie效果较差，需要优化。

        i.  可能的原因有：1.标注不够精确 2.基线模型能力不足
            3.微调数据量不够

    b.  针对上述可能的原因，进行逐个实验，以期望达到预期的结果。

## 周报：5.15-5.19

1.  完成的工作：

    a.  实验模型效果，包括对微调数据进行标注校对、剔除难度较大的样本、更换uie-large版本等。

    b.  对1万条机器标注训练数据进行微调，测试识别效果。之前又收集了100条左右不同格式的样本，进行人工标注，然后继续微调模型。将5维度的分词规则拓展到6维度，并保持原有的精度。

    c.  完成新模型的部署，开放接口。对模型推理开启加速操作：使用单精度运算、使用更快的预处理操作、以及开启多卡运行，实现负载均衡。

    d.  完成69万条云端数据的读取和离线的实体识别，包括以结构化的方式进行结果的保存。

2.  之后的工作：

    a.  优化识别效果

    b.  收集负例数据

    c.  研究关于降低模型复杂度、提高推理速度等方面的问题

## 周报：5.22-5.26

1.  完成的工作：

    a.  提升模型识别精度。在经过两周的探索性实验后，将收集到的负例以及现有的训练数据进行打散重新组合，并且经过细致的预处理和筛选，送入基础预训练模型重新训练。

    b.  从专用测试数据中按照一些特殊规则采样，抽取出百来条数据进行人工标注，用作模型测试集。意在模拟真实业务场景中对困难样本进行识别的情况，计算和统计模型精度，对负例进行查看分析。

2.  之后的工作：

    a.  对数据库中的69万条数据进行离线识别。

    b.  完成持续学习的流程：收集使用时遇到的负例，凑够某个数量级后对模型进行训练改进效果。

## 周报：5.29-6.2

1.  完成的工作：

    a.  将先前带有标注的训练数据中，筛选出单体药店这种格式与标准数据差别较大的数据，进行细致的预处理与筛选。然后将其与其他预处理过的训练数据混合打散，对uie-base模型重新训练。

    b.  完成对69万数据的离线实体识别，69万入参+别名总共84万。以批量的方式执行，并对每个步骤做好检查点保存，以防止程序出错导致结果丢失。具体做法如下：

        i.  读取云端数据库数据，保存到本地。进行预处理以及将每条别名分别添加到原始表中。

        ii. 设定batch数量，以次送入模型进行推理，并将batch的推理结果保存入jsonlines文件中。

        iii. 按照最终结果的获取规则，将模型推理结果转换为结构化的数据格式，按批量写入jsonLines文件。

2.  之后的工作：

    a.  对商品数据的项目的文档进行细致地学习

    b.  对之前的模型推理结果做错误分析，研究之后的精度提升方案

## 周报：6.5-6.9

1.  完成的工作：

    a.  对先前筛选出的"实体长度拼接后不等于原始文本"的错误案例进行统计分析，找出其中的一些特征规律，以及对未来提升效果的启示。

    b.  对生产企业、商业公司数据进行离线推理，并得到结果

2.  之后的工作：

    a.  校对"算法实践项目_书籍"文档

    b.  继续分析错误案例

## 周报：6.12-6.16

1.  完成的工作：

    a.  将现有的实体识别数据从药店、生产企业、商业公司，扩展到能够准确识别医疗机构

    b.  调用chatgptAPI给训练数据进行标注，并对标注过的数据进行后处理，其中包括格式转换、标注校对等

    c.  校对"算法实践项目_书籍"，完成了一半的校对工作

2.  之后的工作：

    a.  检验合并后的模型效果，进行测试

    b.  继续校对书籍

## 周报：6.19-6.25

1.  完成的工作：

    a.  针对合并规则后的模型需要进行统一精度测试的需求，单独整理出一套测试数据集。经由gpt进行预标注，然后导入到标注系统进行人工校对。

    b.  对现有的两个模型（合并规则后的：1小数据集、2大数据集）进行统一精度计算。针对识别效果较弱的类别进行训练数据的调整

    c.  训练新版本模型

2.  之后的工作：

    a.  对新模型进行负例分析，探索之后的优化方向

## 周报：6.26-6.30

1.  完成的工作：

    a.  以上周规则合并后得到的新模型，进行负例分析，形成循坏流程逐步优化模型精度。

    b.  探讨标注规则，形成统一的标注共识，不断校对训练集和测试集的标注。

    c.  使用GPT进行更多数据的标注，校对后制作成训练集，进行模型训练。目前得到了准确率为90%的模型。

    d.  重构离线数据识别的流程代码，跑离线识别数据

2.  之后的工作：

    a.  完成医疗机构模型的上线

    b.  探索商品对码，使用向量库检索流程

## 周报：7.3-7.7

1.  完成的工作：

    a.  将剩余的医疗机构数据进行了统一识别，并按照先前讨论出的统一规则对识别结果的整理代码进行了修改和优化。

    b.  整理了实体识别的所有流程材料，并对ChatGPT与现有流程中的结合也进行了经验总结。

    c.  对向量库检索匹配的相关技术栈进行了部分学习与调研

2.  之后的工作：

    a.  对商品对码向量库匹配检索的自动化流程进行探索

    b.  对使用哪些模型来进行Embedding进行测试实验

## 周报：7.10-7.14

1.  完成的工作：

    a.  根据商品数据（药品、非药）设计并实现了一套用于向量库存储、检索、召回的流程，并使用对码错误案例进行了统一测试

2.  之后的工作：

    a.  探索针对向量库的使用替换更大更强的模型进行测试

    b.  探索基于现有ES召回的数据进行文本相似度的匹配

## 周报：7.17-7.21

1.  完成的工作：

    a.  基于上周设计的使用向量数据库做检索召回的流程，去分别测试了不同模型以及不同数据库的使用。

> 模型包括：基于sentence-transformer的预训练模型、阿里基于电商数据预训练的coROM双塔编码模型
>
> 数据库包括：pinecone、miluvs，尝试调研了一下ElasticSearch
> 7.x版本向量存储，不太会用

2.  之后的工作：

    a.  测试后续视频生成流程中的每个环节，对比我们自己的现有方法

    b.  继续调研测试向量库检索技术，包括使用热搜词query，扩展数据量

    c.  辅助实体识别数据标注

## 周报：7.24-7.28

1.  完成的工作：

    a.  完成多个向量数据库的流程构建，包括PInecone、Milvus、Qdrant。同时调研了其他数据库包括ElasticSearch、Faiss等

    b.  商品规格对码，实体识别流程。完成打标、训练、测试、分析等步骤

2.  之后的工作：

    a.  继续优化商品规格的实体识别

    b.  继续向量数据库调研工作

## 周报：7.31-8.4

1.  完成的工作：

    a.  商品规格的实体识别模型基本完成，对现有90%以上的数据能够准确识别各种规格

    b.  针对实体识别模型的服务接口进行了压力测试，并对结果进行了统计汇总

2.  之后的工作：

    a.  继续向量数据库调研工作

## 周报：8.7-8.11

1.  完成的工作：

    a.  针对商品规格的实体识别模型，做持续性的优化，逐渐覆盖更多的特殊类型数据

    b.  持续向量数据库调研的工作，从多方面进行评估

2.  之后的工作：

    a.  继续优化实体识别

    b.  继续调研向量数据库的使用

## 周报：8.14-8.18

1.  完成的工作：

    a.  规格实体识别的持续优化。添加负例，标注数据，训练以及部署新模型

    b.  基于UIE模型的信息抽取自动化方案Application，目前进度80%

    c.  素材库相关工作（素材主体识别-图片）的技术调研，待定两种方案：

        i.  使用Image-net 数据集的 1K
            种标签作为基本标签集合，在打标时使用基于此数据集的预训练模型进行分类

        ii. 使用基于中文语料预训练的CLIP模型来进行非固定标签的打标，推理效率测试情况符合要求

2.  之后的工作：

    a.  实体识别的优化

    b.  UIE的自动化方案

    c.  素材库打标

    d.  向量数据库检索

## 周报：8.21-8.25

1.  完成的工作：

    a.  规格实体识别的持续性优化

    b.  图片素材打标的技术调用，流程探索

    c.  UIE模型的自动化web项目
